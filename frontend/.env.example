# Backend HTTP origin used by the frontend API client.
# Default matches Constitution Principle III (localhost:3000).
VITE_BACKEND_ORIGIN=http://localhost:3000

# Set to "true" to enable the in-browser MSW mocked backend (dev only).
# Prefer running `npm run dev:mock`, which sets this for you.
VITE_USE_MOCK=false
